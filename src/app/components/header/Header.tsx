import { Contact } from "@/components/shared/contact"

const Header = () => {
    return (
        <div>
            <Contact type="header" />
            <header></header>
        </div>
    )
}
export default Header